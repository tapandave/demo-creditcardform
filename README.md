## Credit Card Form With Validation And LocalStorage Support

In the project directory, you need to run:

`npm install` from the root directory 
and then
`npm start`

You will be now able to access it on http://localhost:8081/
